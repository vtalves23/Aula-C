// 3. Leia 5 numeros e calcule a soma apenas dos posivitos.
#include <stdio.h>

int main() {
    int i, numero , soma = 0;
    for (i = 1;i <= 5; i++) {
  if (numero > 0) {
      soma = soma + numero;
}
}
printf("A soma foi de %d",numero);
    return 0;
}